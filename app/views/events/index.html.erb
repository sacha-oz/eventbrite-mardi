<!DOCTYPE html>
<html lang="en">

  <header>
    <div class="jumbotron" >
    <div class="container h-100">
      <div class="row align-items-center">
        	<div class="col-lg-12">
          		<h1 class="display-4 text-primary mt-5 mb-2">Les Evenements</h1>
          		<p class="btn btn-primary"> <%= link_to "Créer un événement",  new_event_path, :class => "text-white" %> </p>
        	</div>
        </div>
      </div>
    </div>
  </header>

  <body>
  	<div class="card-deck">
      <% Event.all.to_a.each do |event| %>
      <div class="col-md-4 mb-5">
        <div class="card h-100">
          <!-- <img class="card-img-top" src="green.jpg" alt=""> -->
          <div class="card-body">
            <h4 class="card-title"> <%= event.title %> </h4>
            <p class="card-text"> débute le : <%= event.start_date.to_date %> </br> durée de l'evenement : <%= event.duration %> minutes </p>
            <p class="card-text"> organisateur : <%= User.find(event.admin_id).first_name %> <%= User.find(event.admin_id).last_name %> </p>
          </div>
          <div class="card-footer d-flex justify-content-between">
            <div class="btn btn-primary"> <%= link_to "Plus d'informations", event_path(event.id), :class => "text-white" %>  </div>
            <div> <span class="badge badge-secondary"><%= event.attendances.to_a.size %> </span> participant(s) </div>
          </div>
        </div>
       </div>
          <% end %>
    </div>
  </body>



</html>